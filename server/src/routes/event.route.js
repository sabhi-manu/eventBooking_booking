import mongoose from "mongoose";

const route = mongoose.Router();

route.post("/create", )
route.get("/getAll", )
route.get("/get/:id", )
route.put("/update/:id", )
route.delete("/delete/:id", )

export default route;